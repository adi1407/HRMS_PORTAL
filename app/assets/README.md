# App icon

- **icon.png** — 1024×1024 PNG: purple gradient background, white letter “A”, three dots beneath it. Used in `app.json` as `"icon": "./assets/icon.png"`.
- **icon.svg** — Source for the same design; edit and re-export to PNG if you change the icon.
